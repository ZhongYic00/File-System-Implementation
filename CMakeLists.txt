cmake_minimum_required(VERSION 3.1)
project(FS)
aux_source_directory(. DIR_SRCS)
find_package(PkgConfig)
  if (PKG_CONFIG_FOUND)
  pkg_check_modules(GTK "fuse3")
endif()
add_executable(FS ${DIR_SRCS})
target_link_libraries(FS fuse3)
